using System.IO;

namespace FuturamaLib.NIF.Structures
{
    public class NiInterpolator : NiObject
    {
        public NiInterpolator(NIFReader file, BinaryReader reader) : base(file, reader)
        {
        }
    }
}